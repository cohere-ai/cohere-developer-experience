---
layout: custom
no-image-zoom: true
description: >-
  Cohere's documentation home — explore guides, references, and deployment docs
  across all of Cohere's products.
---

<style>
  {`

  /* Home landing page only: hide the secondary tab/search bar.
     The second-level bar should only appear once a product is selected.
     This style is scoped to the home page because it lives in index.mdx. */
  #fern-header .fern-header-tabs {
    display: none !important;
  }

  /*Custom Styles*/
  .rm-Header {
    position: sticky;
    top: 0;
    --Header-background: none;
  }

  .landing-outer {
    margin: 0 auto;
    max-width: 1195px;
    flex-grow: 1;
    overflow-y: auto;
    padding: 40px 32px 0 32px;
  }
  @media (min-width: 768px) {
    .landing-outer {
      padding: 40px;
      padding-bottom: 56px;
    }
  }

  /* Product hub */
  .product-hub {
    margin-bottom: 48px;
  }
  .product-hub-heading {
    margin: 8px 0 8px 0;
  }
  .product-hub-sub {
    margin: 0 0 24px 0;
    max-width: 640px;
  }
  .product-cards-grid {
    display: grid;
    grid-template-columns: repeat(1, minmax(0, 1fr));
    gap: 20px;
  }
  @media (min-width: 768px) {
    .product-cards-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 24px;
    }
  }
  @media (min-width: 1024px) {
    .product-cards-grid {
      grid-template-columns: repeat(4, minmax(0, 1fr));
      gap: 24px;
    }
  }
  .product-card {
    border-radius: 8px;
    background-color: #F5F4F2;
    padding: 24px;
    border-top: 3px solid var(--accent, #39594d);
    transition: background-color 0.15s ease;
  }
  .product-card:hover {
    background-color: #E9E6DE;
  }
  :is(.dark) .product-card {
    background-color: #0F0F0F;
  }
  :is(.dark) .product-card:hover {
    background-color: #292929;
  }
  .product-card a {
    display: flex;
    height: 100%;
    flex-direction: column;
    justify-content: flex-start;
    gap: 14px;
    text-decoration: none;
  }
  .product-card-eyebrow {
    font-size: 0.6875rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: #6b6b6b;
    margin: 0;
  }
  .product-card-title {
    margin: 2px 0 0 0;
    font-size: 1.25rem;
  }
  .product-card-desc {
    margin: 0;
    font-size: 0.9rem;
    opacity: 0.85;
    flex-grow: 1;
  }
  .product-card-cta {
    font-size: 0.875rem;
    font-weight: 500;
    color: #2D4CB9;
  }
  :is(.dark) .product-card-cta {
    color: #4C6EE6;
  }

`}
</style>

<llms-ignore>
export const ProductCard = ({ href, eyebrow, title, description, accent }) => (
  <div className="product-card" style={{ "--accent": accent }}>
    <a href={href} target="_self">
      <div>
        <p className="product-card-eyebrow">{eyebrow}</p>
        <h3 className="product-card-title">{title}</h3>
      </div>
      <p className="product-card-desc">{description}</p>
      <div className="product-card-cta">{"Explore \u2192"}</div>
    </a>
  </div>
);

export const productCards = [
  {
    href: "/platform",
    eyebrow: "Platform",
    title: "Platform",
    description:
      "Command, Embed, and Rerank \u2014 the core API and models for building with Cohere.",
    accent: "#39594d",
  },
  {
    href: "/north",
    eyebrow: "Agentic AI",
    title: "North",
    description:
      "Cohere's secure AI workspace for building agents and automating real work.",
    accent: "#2D4CB9",
  },
  {
    href: "/model-vault",
    eyebrow: "Deployment",
    title: "Model Vault",
    description:
      "Cohere-managed, single-tenant inference environments for your models.",
    accent: "#9A4C2E",
  },
  {
    href: "/compass",
    eyebrow: "Retrieval",
    title: "Compass",
    description:
      "Enterprise search and retrieval to power grounded, accurate answers.",
    accent: "#5B3E8E",
  },
];

<div className="landing-outer">
  <div className="product-hub">
    <h1 className="product-hub-heading">Cohere Documentation</h1>
    <p className="p-lg product-hub-sub">
      Explore guides, references, and deployment docs across all of Cohere's products.
    </p>
    <div className="product-cards-grid">
      {productCards.map((card) => (
        <ProductCard key={card.title} {...card} />
      ))}
    </div>
  </div>
</div>
</llms-ignore>
