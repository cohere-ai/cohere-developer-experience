---
title: Supported Models
slug: "docs/model-vault/encrypted/supported-models"

hidden: false
description: "Which Cohere models are available in Model Vault Encrypted, the supported confidential-computing GPUs, and the isolating architecture."
keywords: "model vault encrypted, supported models, confidential computing GPU, zero data retention, cohere"
---

## Architecture

Model Vault Encrypted runs on the same isolated, single-tenant, Cohere-managed architecture as a
[Standard Vault](/docs/model-vault/standard), and adds on top:

- **Confidential computing**: inference runs inside hardware-backed trusted execution environments (a confidential VM such as Intel TDX or AMD SEV-SNP on the CPU, and an NVIDIA GPU in confidential-computing mode), so data stays protected **in use**, not just in transit and at rest. See the [Security Model](/docs/model-vault/encrypted/security-model) and [Confidential Computing Primer](/docs/model-vault/encrypted/confidential-computing).

<Note>
For how data is protected in transit, at rest, and in use, and how Zero Data Retention (ZDR) is
handled, see [Encryption & Key Management](/docs/model-vault/encrypted/encryption-key-management).
</Note>

## Supported models

Encrypted Vaults support the following model families:

| Model family | Type |
| --- | --- |
| Embed | Embeddings |
| Rerank | Reranker |
| Compass | Embed + Rerank + Vision Parser bundle |
| North Mini Code | Generative (code) |
| Cohere Transcribe | Speech recognition (ASR) |
| Customized Cohere models | Varies |

The families above are what we support in the Model Vault Encrypted **beta**, and we are expanding this
support over time, so availability can differ from a
[Standard Vault](/docs/model-vault/standard/supported-models). To confirm availability for a specific
model, check the model dropdown when [creating a vault](/docs/model-vault/creating-a-vault) with the
Encrypted option selected, or contact Cohere.

## Supported GPUs

Encrypted Vaults run on confidential-computing-capable NVIDIA GPUs. To confirm the
options available for your model, capacity, and region, contact Cohere.

For pricing per model and performance tier, see
[Model Vault Encrypted Pricing](/docs/model-vault/encrypted/pricing).

## Related pages

- [Creating a Vault](/docs/model-vault/creating-a-vault)
- [Standard Vault supported models](/docs/model-vault/standard/supported-models)
- [Security Model](/docs/model-vault/encrypted/security-model)
