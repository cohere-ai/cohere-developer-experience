---
title: Calling a Vault over the API
slug: "docs/model-vault/api-access"

hidden: false
description: "Use your vault endpoint URL and model name to make inference requests with the Cohere SDK."
keywords: "model vault, api, endpoint, inference, cohere sdk"
---

Once your Vault is set up in the dashboard, use the **Vault endpoint URL** and **model name** shown in
the model card (see [Managing Vaults](/docs/model-vault/managing-vaults)) in your API calls.

Point the Cohere SDK at your vault endpoint:

```python PYTHON
import cohere

co = cohere.ClientV2(
    api_key="<COHERE_API_KEY>",
    base_url="<YOUR_VAULT_ENDPOINT_URL>",
)

response = co.chat(
    model="<YOUR_VAULT_MODEL_NAME>",
    messages=[{"role": "user", "content": "Hello from Model Vault!"}],
)

print(response.message.content[0].text)
```

You'll authenticate with an API key — see [API Keys](/docs/model-vault/api-keys).

This page is the **shared baseline** that applies to every vault. The differences for each vault type
are covered separately:

- [Using the API with a Standard Vault](/docs/model-vault/standard/api-usage)
- [Using the API with an Encrypted Vault](/docs/model-vault/encrypted/api-usage)

<Note>
For **Encrypted** vaults, the client also verifies the deployment's
[remote attestation](/docs/model-vault/encrypted/attestation) before sending data and fails closed on
mismatch. See [Verifying Your Deployment](/docs/model-vault/encrypted/verifying-deployment) and
[Using the API with an Encrypted Vault](/docs/model-vault/encrypted/api-usage).
</Note>

## Installing the SDK

{/* TODO: install snippets for the Python and TypeScript SDKs and minimum versions. */}

## Endpoint URL and authentication

{/* TODO: where to find the vault endpoint URL and model name (model card), and how `base_url` + API
key combine. Confirm auth model for vault endpoints. */}

## Chat, Embed, and Rerank

The chat example above points the SDK at your vault. For Embed and Rerank examples, see
[Embeddings & Reranking](/docs/model-vault/embeddings-and-reranking).

## Streaming

{/* TODO: streaming chat example against a vault endpoint. */}

## OpenAI- and Anthropic-compatible APIs

{/* TODO: confirm availability; if supported, show the compatibility base paths and a minimal example
for each. Frame as "confirm availability" until verified. */}

## TypeScript and cURL

{/* TODO: equivalent minimal examples in TypeScript and cURL for the chat request shown above. */}

## Next steps

- [API Keys](/docs/model-vault/api-keys)
- [Embeddings & Reranking](/docs/model-vault/embeddings-and-reranking)
- [Rate Limits & Quotas](/docs/model-vault/rate-limits)
- [Monitoring](/docs/model-vault/monitoring)
