x-internal: true
allOf:
  - $ref: './ChatStreamEvent.yaml'
  - type: object
    required:
      - tool_calls
    properties:
      tool_calls:
        type: array
        items:
          $ref: './ToolCall.yaml'
