description: |
  Represents tool result in the chat history.
required:
  - role
properties:
  role:
    $ref: './ChatRole.yaml'
  tool_results:
    type: array
    items:
      $ref: './ToolResult.yaml'
