allOf:
  - $ref: './ChatStreamEvent.yaml'
  - type: object
    required:
      - citations
    properties:
      citations:
        description: |
          Citations for the generated reply.
        type: array
        x-fern-audiences: ['public']
        items:
          $ref: './ChatCitation.yaml'
