type: object
required:
  - id
  - prompt_tokens
  - completion_tokens
  - raw_prompt_tokens
properties:
  id:
    type: string
    x-fern-audiences: ['public']
  prompt_tokens:
    description: Probabilities for tokens in the request prompt
    type: array
    x-fern-audiences: ['public']
    items:
      $ref: './TokenLikelihood.yaml'
  completion_tokens:
    description: Probabilities for tokens in the request completion
    type: array
    x-fern-audiences: ['public']
    items:
      $ref: './TokenLikelihood.yaml'
  raw_prompt_tokens:
    description: Probabilities for tokens in the request raw_prompt
    type: array
    x-fern-audiences: ['public']
    items:
      $ref: './TokenLikelihood.yaml'
  meta:
    $ref: './ApiMeta.yaml'
    x-fern-audiences: ['public']
