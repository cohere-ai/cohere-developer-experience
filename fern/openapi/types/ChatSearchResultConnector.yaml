description: |
  The connector used for fetching documents.
type: object
required:
  - id
properties:
  id:
    description: |
      The identifier of the connector.
    type: string
    x-fern-audiences: ['public']
    minLength: 1
