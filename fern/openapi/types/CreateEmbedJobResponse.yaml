type: object
required:
  - job_id
description: Response from creating an embed job.
properties:
  job_id:
    type: string
    x-fern-audiences: ['public']
  meta:
    $ref: './ApiMeta.yaml'
    x-fern-audiences: ['public']
