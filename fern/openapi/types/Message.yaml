oneOf:
  - $ref: './ChatMessage.yaml'
  - $ref: './ToolMessage.yaml'
discriminator:
  propertyName: role
  mapping:
    CHATBOT: './ChatMessage.yaml'
    SYSTEM: './ChatMessage.yaml'
    USER: './ChatMessage.yaml'
    TOOL: './ToolMessage.yaml'