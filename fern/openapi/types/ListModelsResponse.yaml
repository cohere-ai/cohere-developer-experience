type: object
required:
  - models
properties:
  models:
    type: array
    x-fern-audiences: ['public']
    items:
      $ref: './GetModelResponse.yaml'
  next_page_token:
    type: string
    x-fern-audiences: ['public']
    description: A token to retrieve the next page of results. Provide in the page_token parameter of the next request.
