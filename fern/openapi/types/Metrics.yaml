type: object
properties:
  finetune_dataset_metrics:
    $ref: './FinetuneDatasetMetrics.yaml'
  embed_data:
    x-internal: true
    type: object
    properties:
      fields:
        description: the fields in the dataset
        x-internal: true
        type: array
        items:
          x-internal: true
          type: object
          properties:
            name:
              description: the name of the field
              x-internal: true
              type: string
            count:
              description: the number of times the field appears in the dataset
              x-internal: true
              type: number
