description: Response in content type stream when `stream` is `true` in the request parameters. Generation tokens are streamed with the GenerationStream response. The final response is of type GenerationFinalResponse.
oneOf:
  - $ref: './GenerateStreamText.yaml'
  - $ref: './GenerateStreamEnd.yaml'
  - $ref: './GenerateStreamError.yaml'
discriminator:
  propertyName: event_type
  mapping:
    'text-generation': './GenerateStreamText.yaml'
    'stream-end': './GenerateStreamEnd.yaml'
    'stream-error': './GenerateStreamError.yaml'
