allOf:
  - $ref: './ChatStreamEvent.yaml'
  - type: object
    properties:
      search_results:
        description: |
          Conducted searches and the ids of documents retrieved from each of them.
        type: array
        x-fern-audiences: ['public']
        minItems: 0
        items:
          $ref: './ChatSearchResult.yaml'
      documents:
        description: |
          Documents fetched from searches or provided by the user.
        type: array
        x-fern-audiences: ['public']
        minItems: 0
        items:
          $ref: './ChatDocument.yaml'
