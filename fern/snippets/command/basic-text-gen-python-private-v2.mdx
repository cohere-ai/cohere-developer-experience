```python PYTHON
import cohere

co = cohere.ClientV2(
    api_key="",
    base_url="YOUR_DEPLOYMENT_URL"
)

response = co.chat(
    model="model", 
    messages=[{"role": "user", "content": "Hello world!"}]
)

print(response)
```